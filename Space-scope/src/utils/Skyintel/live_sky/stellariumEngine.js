/**
 * Stellarium Engine Stub
 * IFRAME MODE ACTIVE
 * ----------------------
 * This file is intentionally inactive while using
 * hosted Stellarium Web via iframe.
 * 
 * Real engine integration can replace this later
 * without changing ObjectView or SkyViewer APIs.
 */

export function createEngine() {
  console.warn(
    "[StellariumEngine] Engine disabled (iframe mode active)"
  );
}

export function destroyEngine() {}

export function setTarget() {}

export function setFov() {}
